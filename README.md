# wikipedia-sidebar-sandbox
To convert Wikipedia Sidebar Templates cross language, one has to compare the existing template against changed code with that of the original English code importing from. So I designed this table to compare the sidebar renders in a single table.
